# Images Folder

Put website images in this folder.

Usage in React components:
- `/images/your-image.jpg`
- `/images/hero/banner.webp`

Because this is inside `public/`, files are served as static assets.
